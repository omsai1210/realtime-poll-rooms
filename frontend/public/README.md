Place your background image file (from the attachment) here and name it `background.jpg` so the app can load it at `/background.jpg`.

Example:
- frontend/public/background.jpg

If you prefer a different name or path, update `frontend/src/index.css` to match the location.